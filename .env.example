# Copy to .env (which is gitignored) and adjust values for your environment.

# TheHive session/application secret.
# TheHive 5.5+ requires sufficient entropy for HS256 (practically: 32+ bytes).
# Generate one for local dev (PowerShell example):
#   [Convert]::ToBase64String((1..48 | ForEach-Object { Get-Random -Maximum 256 }) -as [byte[]])
# Use a long random value in real deployments.
THEHIVE_SECRET=REPLACE_WITH_LONG_RANDOM_BASE64_STRING_AT_LEAST_32_BYTES

# BloodHound / database credentials (lab/dev defaults)
BH_ADMIN_PASSWORD=change-me
NEO4J_AUTH=neo4j/bloodhoundcommunityedition
BH_POSTGRES_USER=bloodhound
BH_POSTGRES_PASSWORD=bloodhoundcommunityedition
BH_POSTGRES_DATABASE=bloodhound

# BloodHound trusted-upstream auth bypass (used by nginx internal route)
# Set to real values in production.
BLOODHOUND_INTERNAL_USER=thehive

# Option A (preferred for lab/dev): let nginx bootstrap a session token from BloodHound using admin creds
BLOODHOUND_INTERNAL_LOGIN_USERNAME=admin
BLOODHOUND_INTERNAL_LOGIN_SECRET=change-me

# Option B (prod): set a pre-generated session token explicitly
BLOODHOUND_INTERNAL_TOKEN=

# MidPoint / database credentials (lab/dev defaults)
MP_POSTGRES_USER=midpoint
MP_POSTGRES_PASSWORD=change-me
MP_POSTGRES_DB=midpoint
MIDPOINT_USERNAME=administrator
MIDPOINT_PASSWORD=change-me
